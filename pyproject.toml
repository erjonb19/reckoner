[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "reckoner"
version = "0.1.0"
description = "Hospital contract and reimbursement modeling for New York: APR-DRG payment calculation, rate benchmarking, and price transparency reconciliation."
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "ijson>=3.2",
    "pyarrow>=16.0",
    "openpyxl>=3.1",
    "PyYAML>=6.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "respx>=0.21",
    "ruff>=0.6",
    "mypy>=1.11",
    "types-PyYAML",
]

[project.scripts]
nypt-discover = "discovery.cli:main"

[tool.setuptools]
package-dir = { "" = "src" }
# The job entrypoint is a top-level module, not a package, so packages.find does
# not see it. Without this the image builds cleanly and then dies at run time
# with ModuleNotFoundError -- caught by inspecting the wheel rather than by
# trusting the build to have done the obvious thing.
py-modules = ["reckoner_job"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
asyncio_mode = "auto"
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py311"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "ANN", "RUF"]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["ANN001", "ANN201"]

[tool.ruff.lint.isort]
known-first-party = ["agents", "benchmark", "discovery", "hospital", "model", "payer", "reconcile"]

[tool.mypy]
python_version = "3.11"
strict = true
mypy_path = "src"
packages = ["agents", "benchmark", "discovery", "hospital", "model", "payer", "reconcile"]

[[tool.mypy.overrides]]
module = ["ijson.*", "openpyxl.*", "pyarrow.*"]
ignore_missing_imports = true
